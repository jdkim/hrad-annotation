<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "SCE Annotation System" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm">
      <div class="container mx-auto px-4">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <%= link_to "SCE Annotation System", root_path, class: "text-xl font-bold text-gray-900" %>
          </div>
          <div class="flex items-center space-x-4">
            <% if logged_in? %>
              <span class="text-sm text-gray-600">
                <%= current_user.name || current_user.email %>
              </span>
              <%= button_to "Logout", logout_path, method: :delete, class: "text-sm text-gray-600 hover:text-gray-900" %>
            <% else %>
              <%= link_to "Login", login_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative container mx-auto mt-4" role="alert">
        <span class="block sm:inline"><%= notice %></span>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative container mx-auto mt-4" role="alert">
        <span class="block sm:inline"><%= alert %></span>
      </div>
    <% end %>

    <!-- Main Content -->
    <main>
      <%= yield %>
    </main>
  </body>
</html>
